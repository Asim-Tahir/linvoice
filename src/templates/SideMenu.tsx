import { StyledSideMenu } from "@/styled";

export default function SideMenu(): React.ReactElement {
  return (
    <StyledSideMenu>
      <h2>Linvoice</h2>
      <div></div>
    </StyledSideMenu>
  );
}
